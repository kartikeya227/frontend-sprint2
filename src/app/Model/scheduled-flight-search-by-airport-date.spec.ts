import { ScheduledFlightSearchByAirportDate } from './scheduled-flight-search-by-airport-date';

describe('ScheduledFlightSearchByAirportDate', () => {
  it('should create an instance', () => {
    expect(new ScheduledFlightSearchByAirportDate()).toBeTruthy();
  });
});
